# 音乐目录

上游仓库在这里放了一批 mp3（内置音乐播放页用）。**本项目刻意不包含这些文件**：
它们是第三方商业音乐，随仓库分发不合适。

自己放：把 mp3 拷进本目录，再照下面格式写 `music.json`（路由是 `/music/<文件名>`）：

```json
[
  { "name": "曲名 - 作者", "url": "/music/example.mp3" }
]
```

文件名含空格或中文时，`url` 里要用百分号编码（`encodeURIComponent` 的结果）。
